# Historical animation reference

The prior suite animation grammar is retired with its independent shell.
Current behavior follows [the product contract](../../docs/PRODUCT.md), the
active Map implementation and system/browser reduced-motion preferences.
Animation must not manufacture agent activity or become an execution signal.
