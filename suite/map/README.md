# Historical Map modules

These are compatibility assets for the retired suite shell. Current Map source
is [map/v1](../../map/v1/) and its active application integration is described
in [ARCHITECTURE.md](../../ARCHITECTURE.md). Inspect actual import and resource
consumers before removing retained modules. Do not treat historical animation,
module-size or migration notes as the current implementation plan.
