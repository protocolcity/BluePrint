# Consolidation status

The current application is `overview/v1`, with the document Map in `map/v1`. Local release candidates are built and activated through `docs/operations/DEPLOYMENT.md`. The running version is identified by the selected workspace deployment receipt and `/api/operations`; source changes alone do not indicate deployment.

The baseline is public BluePrint ac1302f. Recovered `protocolcity/` and `suite/` modules originated in the installed 0.1.47 distribution. Their utilities and compatibility helpers are being reconciled; legacy service starts route to the current lifecycle. Host-specific legacy wall fixtures and map shortcuts have been retired from this source. Former private histories remain outside this canonical checkout and must not be merged or published wholesale.

Implemented boundaries include explicit workspace-local store reads, protected document paths, project-scoped engine writes, honest source freshness, GitHub evidence separate from agent liveness, dated work-order calendar events, and an isolated installed release lifecycle. WorkLane and WorkForce have independent packages and data homes. Starting the interface does not create or hire workers.

Local validation includes Overview/Map regressions, disposable WorkLane write integration, installed wheel smoke, and live browser checks. Exact results and host deployment receipts belong to the workspace audit rather than distributable source. Public publication remains a separate source review and release step.
