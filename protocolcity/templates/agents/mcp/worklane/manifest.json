{
  "id": "worklane",
  "description": "WorkLane board (claim, close, ready)",
  "transport": "stdio",
  "command": "python3",
  "args": [
    "-m",
    "worklane.mcp",
    "--author",
    "you"
  ],
  "env": {
    "TP_AGENT_ID": "${TP_AGENT_ID:-you}",
    "WORKLANE_RUNTIME_DIR": "{{WORKSPACE_ROOT}}/.protocolcity/worklane"
  },
  "env_required": [],
  "capability": "mutating",
  "risk": "board write — claim/close/label; not money",
  "seats": [
    "*"
  ],
  "level": "L0",
  "vendor_locked": false,
  "enabled": true,
  "notes": "Bottle entrypoint (python3 -m worklane.mcp) with runtime under .protocolcity/worklane. Legacy sibling checkout (…/worklane/.venv/bin/python) and the prior package-module name are rewritten on seed / doctor --fix when the command path is dead. Cursor mcp_auth on a failed local stdio start is not an OAuth grant."
}
